# ========================================================================
# 📋 QUICK START GUIDE
# 
# Before deployment:
# 1. Fill in all fields marked with 📝 (these are required)
# 2. Only edit values between PARAMETERS_START and PARAMETERS_END
#
# ========================================================================
# REQUIRED PARAMETERS (FILL THESE BEFORE DEPLOYMENT)
# ========================================================================
# <<< PARAMETERS_START >>>

AppShortName: "<edit_here>"                      # 📝 e.g. webapp, api, frontend
EnvName: "<edit_here>"                           # 📝 e.g. nprd, nprd-dev, prod
Alb1Name: "<edit_here>"                          # 📝 e.g. webapp-prod-alb-public

# ========================================================================
# OPTIONAL PARAMETERS (adjust based on workload or use defaults)
# ========================================================================
Alb1TargetGroup1Name: ""                         # e.g. webapp-prod-tg-frontend
Alb1TargetGroup2Name: ""                         # e.g. webapp-prod-tg-api
Alb1TargetGroup3Name: ""                         # e.g. webapp-prod-tg-admin
Alb2Name: ""                                     # e.g. webapp-prod-alb-internal
Alb2TargetGroup1Name: ""                         # e.g. webapp-prod-tg-backend
Alb2TargetGroup2Name: ""                         # e.g. webapp-prod-tg-worker
Alb2TargetGroup3Name: ""                         # e.g. webapp-prod-tg-scheduler
Alb3Name: ""                                     # e.g. webapp-prod-alb-microservices
Alb3TargetGroup1Name: ""                         # e.g. webapp-prod-tg-service1
Alb3TargetGroup2Name: ""                         # e.g. webapp-prod-tg-service2
Alb3TargetGroup3Name: ""                         # e.g. webapp-prod-tg-service3
Alb4Name: ""                                     # e.g. webapp-prod-alb-external
Alb4TargetGroup1Name: ""                         # e.g. webapp-prod-tg-public-api
Alb4TargetGroup2Name: ""                         # e.g. webapp-prod-tg-webhooks
Alb4TargetGroup3Name: ""                         # e.g. webapp-prod-tg-integration
Team1SNSTopicArn: ""                             # e.g. arn:aws:sns:region:account:ops-team
Team2SNSTopicArn: ""                             # e.g. arn:aws:sns:region:account:dev-team
Team3SNSTopicArn: ""                             # e.g. arn:aws:sns:region:account:infra-team

# <<< PARAMETERS_END >>>
# ========================================================================
# 📖 PARAMETER REFERENCE — DO NOT EDIT BELOW THIS LINE
# ========================================================================

# --- APP SHORT NAME ---
# AppShortName: Application identifier for resource naming
#
# Purpose:
# - Creates alarm names: {AppShortName}-{EnvName}-{ALBName}-*
# - Used for alarm organization and filtering
#
# Format: Lowercase alphanumeric
# Example: webapp, api, frontend
#
# Finding Your Value:
# - Use your organization's application naming convention
# - Should match naming used in other infrastructure components

# --- ENVIRONMENT NAME ---
# EnvName: Environment designation for resource naming and configuration
#
# Purpose:
# - Used in resource naming
# - Determines deletion policy (Retain for prod, Delete for nprd)
#
# Allowed Values (Non-Production):
# - nprd, nprd-dev, nprd-sit, nprd-sit1, nprd-sit2, nprd-sit3, nprd-sit-a,
#   nprd-sit-b, nprd-sit-c, nprd-uat, nprd-uat-a, nprd-uat-b, nprd-uat-c,
#   nprd-pt, nprd-pp, nprd-pp-a, nprd-pp-b, nprd-pp-c
#
# Allowed Values (Production):
# - prod, prod-a, prod-b, prod-c
#
# Environment-Specific Behavior:
# Production environments (prod*):
# - DeletionPolicy: Retain (alarms preserved on stack deletion)
#
# Non-Production environments (nprd*):
# - DeletionPolicy: Delete (alarms deleted on stack deletion)
#
# Finding Your Value:
# - Coordinate with your infrastructure team
# - Should match environment naming used in related resources

# --- ALB 1 NAME ---
# Alb1Name: Application Load Balancer 1 name for monitoring
#
# Purpose:
# - Specifies primary ALB for CloudWatch alarms
# - Creates alarms for 5XX errors
#
# Format: LoadBalancer name
# Example: webapp-prod-alb-public, api-dev-alb
#
# Finding Your Value (AWS Console):
# 1. Navigate to EC2 Console → Load Balancers
# 2. Find your Application Load Balancer
# 3. Copy the load balancer name

# --- ALB 1 TARGET GROUP 1 NAME (OPTIONAL) ---
# Alb1TargetGroup1Name: Target group 1 name for ALB 1
#
# Purpose:
# - Creates alarms for unhealthy hosts, 5XX errors, response time
# - Optional - leave empty if not monitoring target group
#
# Format: TargetGroup ARN suffix or empty string
# Example: targetgroup/webapp-prod-tg-frontend/abc123def456 or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to EC2 Console → Target Groups
# 2. Find your target group
# 3. Copy the full ARN from details tab
# 4. Leave empty ("") if not monitoring this target group

# --- ALB 1 TARGET GROUP 2/3 NAMES (OPTIONAL) ---
# Alb1TargetGroup2Name, Alb1TargetGroup3Name: Additional target groups for ALB 1
#
# Purpose:
# - Creates same set of alarms as Target Group 1
# - Optional - leave empty if not monitoring
#
# Format: TargetGroup ARN suffix or empty string
# Example: targetgroup/webapp-prod-tg-api/abc123def456 or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to EC2 Console → Target Groups
# 2. Find your target group
# 3. Copy the full ARN from details tab
# 4. Leave empty ("") if not monitoring

# --- ALB 2/3/4 NAMES (OPTIONAL) ---
# Alb2Name, Alb3Name, Alb4Name: Additional Application Load Balancers
#
# Purpose:
# - Monitor additional ALBs (optional)
# - Creates same set of alarms as ALB 1
#
# Format: LoadBalancer name or empty string
# Example: webapp-prod-alb-internal or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to EC2 Console → Load Balancers
# 2. Find your Application Load Balancer
# 3. Copy the load balancer name
# 4. Leave empty ("") if not monitoring additional ALBs

# --- ALB 2/3/4 TARGET GROUP NAMES (OPTIONAL) ---
# Alb2TargetGroup1/2/3Name, Alb3TargetGroup1/2/3Name, Alb4TargetGroup1/2/3Name
#
# Purpose:
# - Monitor target groups for additional ALBs
# - Creates alarms for unhealthy hosts, 5XX errors, response time
#
# Format: TargetGroup ARN suffix or empty string
# Example: targetgroup/webapp-prod-tg-backend/abc123def456 or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to EC2 Console → Target Groups
# 2. Find your target group
# 3. Copy the full ARN from details tab
# 4. Leave empty ("") if not monitoring

# --- TEAM 1 SNS TOPIC ARN (OPTIONAL) ---
# Team1SNSTopicArn: SNS Topic ARN for Team 1 notifications
#
# Purpose:
# - Sends alarm notifications to Team 1 (typically L2 Operations)
# - All alarms send to this topic when triggered
#
# Format: arn:aws:sns:region:account-id:topic-name or empty string
# Example: arn:aws:sns:ap-southeast-1:123456789012:ops-team or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to SNS Console → Topics
# 2. Find the topic for Team 1 / L2 Operations
# 3. Copy the ARN
# 4. Leave empty ("") if not using Team 1 notifications

# --- TEAM 2 SNS TOPIC ARN (OPTIONAL) ---
# Team2SNSTopicArn: SNS Topic ARN for Team 2 notifications
#
# Purpose:
# - Sends alarm notifications to Team 2 (typically L3 Support/Development)
# - All alarms send to this topic when triggered
#
# Format: arn:aws:sns:region:account-id:topic-name or empty string
# Example: arn:aws:sns:ap-southeast-1:123456789012:dev-team or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to SNS Console → Topics
# 2. Find the topic for Team 2 / L3 Support
# 3. Copy the ARN
# 4. Leave empty ("") if not using Team 2 notifications

# --- TEAM 3 SNS TOPIC ARN (OPTIONAL) ---
# Team3SNSTopicArn: SNS Topic ARN for Team 3 notifications
#
# Purpose:
# - Sends alarm notifications to Team 3 (typically Infrastructure/Platform)
# - All alarms send to this topic when triggered
#
# Format: arn:aws:sns:region:account-id:topic-name or empty string
# Example: arn:aws:sns:ap-southeast-1:123456789012:infra-team or ""
#
# Finding Your Value (AWS Console):
# 1. Navigate to SNS Console → Topics
# 2. Find the topic for Team 3 / Infrastructure
# 3. Copy the ARN
# 4. Leave empty ("") if not using Team 3 notifications
