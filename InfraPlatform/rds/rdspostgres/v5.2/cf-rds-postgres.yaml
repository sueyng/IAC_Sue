AWSTemplateFormatVersion: 2010-09-09
#Description:
Description: Setup for PostgreSQL Instance V5.2
Parameters:

  AppShortName:
    Type: String
    Description: AppShortName

  EnvName:
    Type: String
    Description: Environment
    AllowedValues:
      - nprd
      - nprd-dev
      - nprd-dev1
      - nprd-dev2
      - nprd-dev-a
      - nprd-dev-b
      - nprd-dev-c
      - nprd-sit1
      - nprd-sit2
      - nprd-sit3
      - nprd-sit
      - nprd-sit-a
      - nprd-sit-b
      - nprd-sit-c
      - nprd-uat
      - nprd-uat-a
      - nprd-uat-b
      - nprd-uat-c
      - nprd-pt
      - nprd-pp
      - nprd-pp-a
      - nprd-pp-b
      - nprd-pp-c
      - prod
      - prod-a
      - prod-b
      - prod-c
  
  VpcId:
    Type: AWS::EC2::VPC::Id
    Description: VpcId of your existing Virtual Private Cloud (VPC)

  AvailabilityZone:
    Type: AWS::EC2::AvailabilityZone::Name
    Description: Select the Primary Region for PostgreSQL Deployment.
    AllowedValues:
      - ap-southeast-1a
      - ap-southeast-1b
      - ap-southeast-1c

# Subnet CIDR parameters
  VPCSubnetCidrAppAZ1:
    Type: String
    Description: AZ1 App Subnet
  
  VPCSubnetCidrAppAZ2:
    Type: String
    Description: AZ2 App Subnet

  VPCSubnetCidrAppAZ3:
    Type: String
    Description: AZ3 App Subnet

  VPCSubnetCidrAppAZ4:
    Type: String
    Description: AZ4 App Subnet

  VPCSubnetCidrAppAZ5:
    Type: String
    Description: AZ5 App Subnet

  VPCSubnetCidrAppAZ6:
    Type: String
    Description: AZ6 App Subnet

  # DB Subnet ID parameters
  DBSubnetIds:
    Type: List<AWS::EC2::Subnet::Id>
    Description: List of DB Subnet Ids

  DBSubnetCidrAZ1:
    Type: String
    Description: DB Subnet CIDR for AZ1
    Default: ""
  
  DBSubnetCidrAZ2:
    Type: String
    Description: DB Subnet CIDR for AZ2
    Default: ""

  DBSubnetCidrAZ3:
    Type: String
    Description: DB Subnet CIDR for AZ3 (optional)
    Default: ""

  DBSubnetCidrAZ4:
    Type: String
    Description: DB Subnet CIDR for AZ4 (optional)
    Default: ""

  DBSubnetCidrAZ5:
    Type: String
    Description: DB Subnet CIDR for AZ5 (optional)
    Default: ""

  DBSubnetCidrAZ6:
    Type: String
    Description: DB Subnet CIDR for AZ6 (optional)
    Default: ""

  UseExistingSubnetGroup:
    Type: String
    Description: Whether to use an existing DB subnet group ('true') or create a new one ('false'). Set to 'true' for v3/v4/v5 to v5.1 upgrade to avoid DBInstance replacement (the v5.1 default subnet group name differs from earlier versions — see ExistingSubnetGroupName).
    AllowedValues: ['true', 'false']
    Default: 'false'

  ExistingSubnetGroupName:
    Type: String
    Description: Name of the existing DB subnet group (used when UseExistingSubnetGroup is 'true'). For v3 → v5.1 upgrade, provide the v3 subnet group name (typically the AppShortName-EnvName-subnetgroup pattern, without the '-postgres-' segment introduced in v5.1).
    Default: ''

  DeploymentServer:
    Type: String
    Description: Deployment Server IP

  MultiAZ:
    Type: String
    Description: Multi-AZ deployment (true / false)
    AllowedValues: ['true', 'false']

  PgsqlDBParameterGroupFamily:
    Type: String
    Description: Parameter group family, eg, postgres16

  PgsqlDBInstanceClass:
    Type: String
    Description: The database instance type, eg - db.m6i.xlarge

  PgsqlDBEngine:
    Type: String
    Description: Enter the SQL DB Engine Name, se is standard and ee is enterprise
    AllowedValues:
      - postgres

  PgsqlDBMajorEngineVersion:
    Type: String
    Description: Enter the DB Engine version, eg- 16.00
  
  PgsqlEngineVersion:
    Type: String
    Description : Get the latest engine version from the AWS console, eg -16.4
    Default: '16.4'

  PgsqlAllocatedStorage:
    Type: String
    Description: Total DB size

  PgsqlMaxAllocatedStorage:
    Type: String
    Description: Maximum storage for DB instance

  PgsqlStorageType:
    Type: String
    Description: Volume Type

  PgsqlStorageIops:
    Type: String
    Description: Storage IOPS for the allocated storage
    Default: ''

  PgsqlMasterUsernameDev:
    Type: String
    Default: dbadmin
    NoEcho: true

  PgsqlMasterUserPasswordDev:
    Type: String
    Default: dbadmin123
    NoEcho: true

  PgsqlProdDBPort:
    Type: String
    Default: 53341

  PgsqlNProdDBPort:
    Type: String
    Default: 53331

  EnableIAMDatabaseAuthentication:
    Type: String
    Description: Enable IAM database authentication (true / false)
    AllowedValues: ['true', 'false']
    Default: 'false'

# VPC Subnet CIDR parameters
  VpcCidr1:
    Description: VPC CIDR 1 IP Range
    Type: String
    Default: ""

  VpcCidr2:
    Description: VPC CIDR 2 IP Range
    Type: String
    Default: ""

  VpcCidr3:
    Description: VPC CIDR 3 IP Range
    Type: String
    Default: ""

  VpcCidr4:
    Description: VPC CIDR 4 IP Range
    Type: String
    Default: ""

  VpcCidr5:
    Description: VPC CIDR 5 IP Range
    Type: String
    Default: ""

  S3PrefixListId:
    Description: S3 PrefixList ID
    Type: String
    Default: ""

  HCCVpceCidr:
    Description: HCC VPC Endpoint Subnet, Fixed CIDR Range from HCC
    Type: String
    Default: ""

  KMSKeyRotationPeriod:
    Type: Number
    Description: Number of days for KMS key rotation period (default 365 days)
    Default: 365

  # Prefix List for Additional Application Networks
  AdditionalAppPrefixList:
    Type: String
    Description: Optional Prefix List ID for additional application networks (optional)
    Default: ""

Conditions:
  IsProduction: !Or [!Equals [!Ref "EnvName", "prod"], !Equals [!Ref "EnvName", "prod-a"], !Equals [!Ref "EnvName", "prod-b"], !Equals [!Ref "EnvName", "prod-c"]]
  HasVPCSubnetCidrAppAZ3: !Not [!Equals [!Ref VPCSubnetCidrAppAZ3, '']]
  HasVPCSubnetCidrAppAZ4: !Not [!Equals [!Ref VPCSubnetCidrAppAZ4, '']]
  HasVPCSubnetCidrAppAZ5: !Not [!Equals [!Ref VPCSubnetCidrAppAZ5, '']]
  HasVPCSubnetCidrAppAZ6: !Not [!Equals [!Ref VPCSubnetCidrAppAZ6, '']]
  IsMultiAZ: !Equals [!Ref MultiAZ, 'true']
  HasDBSubnetCidrAZ3: !Not [!Equals [!Ref DBSubnetCidrAZ3, '']]
  HasDBSubnetCidrAZ4: !Not [!Equals [!Ref DBSubnetCidrAZ4, '']]
  HasDBSubnetCidrAZ5: !Not [!Equals [!Ref DBSubnetCidrAZ5, '']]
  HasDBSubnetCidrAZ6: !Not [!Equals [!Ref DBSubnetCidrAZ6, '']]
  CreateNewSubnetGroup: !Equals [!Ref UseExistingSubnetGroup, 'false']
  HasAdditionalAppPrefixList: !Not [!Equals [!Ref AdditionalAppPrefixList, '']]
  HasStorageIops: !Not [!Equals [!Ref PgsqlStorageIops, ""]]
  HasVpcCidr3: !Not [!Equals [!Ref VpcCidr3, '']]
  HasVpcCidr2: !Not [!Equals [!Ref VpcCidr2, '']]
  HasVpcCidr4: !Not [!Equals [!Ref VpcCidr4, '']]
  HasVpcCidr5: !Not [!Equals [!Ref VpcCidr5, '']]
  HasHCCVpceCidr: !Not [!Equals [!Ref HCCVpceCidr, ""]]
  HasS3PrefixListId: !Not [!Equals [!Ref S3PrefixListId, ""]]

Resources:
# KMS Key for encrypt the PostgreSQL backups and DB related Files
  pgsqlencryptionkmskey:
    Type: 'AWS::KMS::Key'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      Description: 'KMS key to encrypt DB related files and native backups'
      EnableKeyRotation: true
      RotationPeriodInDays: !Ref KMSKeyRotationPeriod
      PendingWindowInDays: 7
      KeyPolicy:
        Version: 2012-10-17
        Id: key-default-1
        Statement:
          - Sid: Enable IAM Root Permissions
            Effect: Allow
            Principal:
              AWS: !Sub 'arn:aws:iam::${AWS::AccountId}:root'
            Action: 'kms:*'
            Resource: '*'
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"
          
# Alias for the KMS key
  pgsqlencryptionkmskeyalias:
    Type: 'AWS::KMS::Alias'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      AliasName: !Sub 'alias/${AppShortName}-${EnvName}-rdspostg-encryption'
      TargetKeyId: !Ref pgsqlencryptionkmskey

  pgsqlmonitoringencryptionkmskey:
    Type: 'AWS::KMS::Key'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      Description: 'KMS key to encrypt DB related files and native backups'
      EnableKeyRotation: true
      RotationPeriodInDays: !Ref KMSKeyRotationPeriod
      PendingWindowInDays: 7
      KeyPolicy:
        Version: 2012-10-17
        Id: key-default-1
        Statement:
          - Sid: Enable IAM Root Permissions
            Effect: Allow
            Principal:
              AWS: !Sub 'arn:aws:iam::${AWS::AccountId}:root'
            Action: 'kms:*'
            Resource: '*'
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"
            
  pgsqlmonitoringencryptionkmskeyalias:
    Type: 'AWS::KMS::Alias'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      AliasName: !Sub 'alias/${AppShortName}-${EnvName}-rdspostg-monitoring-encryption'
      TargetKeyId: !Ref pgsqlmonitoringencryptionkmskey

  dbcombineds3bucket:
    Type: 'AWS::S3::Bucket'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      BucketName: !Sub '${AppShortName}-${EnvName}-rdspostg-s3store'
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      AccessControl: Private
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"

  s3bucketpolicy:
    Type: AWS::S3::BucketPolicy
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      Bucket: !Ref dbcombineds3bucket
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          - Sid: EnforceTLSv12orHigher
            Effect: Deny
            Principal: '*'
            Action: s3:*
            Resource: 
              - !GetAtt dbcombineds3bucket.Arn
              - !Join ['', [!GetAtt dbcombineds3bucket.Arn, '/*']]
            Condition:
              NumericLessThan:
                s3:TlsVersion: 1.2
          - Sid: AllowSSLRequestsOnly
            Effect: Deny
            Principal: '*'
            Action: s3:*
            Resource: 
              - !GetAtt dbcombineds3bucket.Arn
              - !Join ['', [!GetAtt dbcombineds3bucket.Arn, '/*']]
            Condition:
              Bool:
                aws:SecureTransport: 'false'

#IAM Roles and Policies
  dbenhancedmonitoringrole:
    Type: 'AWS::IAM::Role'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - monitoring.rds.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      Path: /
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonRDSEnhancedMonitoringRole
      RoleName: !Sub '${AppShortName}-${EnvName}-rdspostg-enhancedmonitoring-role'
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"

  combineddbpolicy:
      Type: 'AWS::IAM::ManagedPolicy'
      DeletionPolicy: !If [IsProduction, Retain, Delete]
      UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
      Properties:
        PolicyDocument:
          Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - kms:Decrypt
                - kms:DescribeKey
                - kms:GenerateDataKey
              Resource:
                - !GetAtt pgsqlencryptionkmskey.Arn
            - Effect: Allow
              Action:
                - s3:ListAllMyBuckets
              Resource: "*"
            - Effect: Allow
              Action:
                - s3:GetBucketLocation
                - s3:GetBucketACL
                - s3:ListBucket
              Resource:
                - !GetAtt dbcombineds3bucket.Arn
            - Effect: Allow
              Action:
                - s3:ListMultipartUploadParts
                - s3:AbortMultipartUpload
                - s3:PutObject
              Resource:
                - !Sub '${dbcombineds3bucket.Arn}/*'
            - Effect: Allow
              Action:
                - s3:GetObjectMetaData
                - s3:GetObject
                - s3:PutObject
                - s3:ListMultipartUploadParts
                - s3:AbortMultipartUpload
              Resource:
                - !Sub '${dbcombineds3bucket.Arn}/*'
        ManagedPolicyName: !Sub '${AppShortName}-${EnvName}-rdspostg-policy'

  dbcombineds3role:
    Type: 'AWS::IAM::Role'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - rds.amazonaws.com
            Action:
              - 'sts:AssumeRole'
      Path: /
      ManagedPolicyArns:
        - !Ref combineddbpolicy
      RoleName: !Sub '${AppShortName}-${EnvName}-rdspostg-role'
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"

# Security Group
  pgsqldbsecuritygroup:
    Type: 'AWS::EC2::SecurityGroup'
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      GroupDescription: Security Group for PostgreSQL DB
      GroupName: !Sub '${AppShortName}-${EnvName}-sg-rdspostg-db-postgresql'
      Tags:
        - Key: Name
          Value: !Sub '${AppShortName}-${EnvName}-sg-rdspostg-db-postgresql'
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"
      VpcId: !Ref VpcId
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          CidrIp: !Ref VPCSubnetCidrAppAZ1
          Description: Allow App AZ1 Subnet
        - IpProtocol: tcp
          FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          CidrIp: !Ref VPCSubnetCidrAppAZ2
          Description: Allow App AZ2 Subnet
        - !If
          - HasVPCSubnetCidrAppAZ3
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref VPCSubnetCidrAppAZ3
            Description: Allow App Subnet AZ3
          - !Ref "AWS::NoValue"
        - !If
          - HasVPCSubnetCidrAppAZ4
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref VPCSubnetCidrAppAZ4
            Description: Allow App Subnet AZ4
          - !Ref "AWS::NoValue"
        - !If
          - HasVPCSubnetCidrAppAZ5
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref VPCSubnetCidrAppAZ5
            Description: Allow App Subnet AZ5
          - !Ref "AWS::NoValue"
        - !If
          - HasVPCSubnetCidrAppAZ6
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref VPCSubnetCidrAppAZ6
            Description: Allow App Subnet AZ6
          - !Ref "AWS::NoValue"
        - !If
          - HasAdditionalAppPrefixList
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            SourcePrefixListId: !Ref AdditionalAppPrefixList
            Description: Allow Additional Application Networks
          - !Ref "AWS::NoValue"
        - IpProtocol: tcp
          FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          CidrIp: !Ref DeploymentServer
          Description: Allow Deployment Server to Access the PostgreSQL instance
        - IpProtocol: tcp
          FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          CidrIp: !Ref DBSubnetCidrAZ1
          Description: Allow Database Subnet Traffic
        - IpProtocol: tcp
          FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
          CidrIp: !Ref DBSubnetCidrAZ2
          Description: Allow Database Subnet Traffic
        - !If
          - HasDBSubnetCidrAZ3
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref DBSubnetCidrAZ3
            Description: Allow Database Subnet Traffic
          - !Ref "AWS::NoValue"
        - !If
          - HasDBSubnetCidrAZ4
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref DBSubnetCidrAZ4
            Description: Allow Database Subnet Traffic
          - !Ref "AWS::NoValue"
        - !If
          - HasDBSubnetCidrAZ5
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref DBSubnetCidrAZ5
            Description: Allow Database Subnet Traffic
          - !Ref "AWS::NoValue"
        - !If
          - HasDBSubnetCidrAZ6
          - IpProtocol: tcp
            FromPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            ToPort: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
            CidrIp: !Ref DBSubnetCidrAZ6
            Description: Allow Database Subnet Traffic
          - !Ref "AWS::NoValue"
      SecurityGroupEgress:
        - IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          CidrIp: !Ref VpcCidr1
          Description: VPC CIDR 1 IP Range
        - !If 
          - HasVpcCidr2
          - IpProtocol: tcp
            FromPort: 443
            ToPort: 443
            CidrIp: !Ref VpcCidr2
            Description: VPC CIDR 2 IP Range
          - !Ref "AWS::NoValue"
        - !If
          - HasVpcCidr3
          - IpProtocol: tcp
            FromPort: 443
            ToPort: 443
            CidrIp: !Ref VpcCidr3
            Description: VPC CIDR 3 IP Range
          - !Ref "AWS::NoValue"
        - !If
          - HasVpcCidr4
          - IpProtocol: tcp
            FromPort: 443
            ToPort: 443
            CidrIp: !Ref VpcCidr4
            Description: VPC CIDR 4 IP Range
          - !Ref "AWS::NoValue"
        - !If
          - HasVpcCidr5
          - IpProtocol: tcp
            FromPort: 443
            ToPort: 443
            CidrIp: !Ref VpcCidr5
            Description: VPC CIDR 5 IP Range
          - !Ref "AWS::NoValue"
        - !If
          - HasS3PrefixListId
          - IpProtocol: tcp
            FromPort: 443
            ToPort: 443
            DestinationPrefixListId: !Ref S3PrefixListId
            Description: Allow access to Amazon S3 Endpoint
          - !Ref "AWS::NoValue"
        - !If
          - HasHCCVpceCidr
          - IpProtocol: tcp
            FromPort: 443
            ToPort: 443
            CidrIp: !Ref HCCVpceCidr
            Description: HCC VPC Endpoint Subnet
          - !Ref "AWS::NoValue"      

#PostgreSQL DB Subnet Group
# Skipped when UseExistingSubnetGroup is 'true' (e.g. v3/v4/v5 → v5.1 upgrade preserving existing subnet group).
  pgsqldbsubnetgroup:
    Type: AWS::RDS::DBSubnetGroup
    Condition: CreateNewSubnetGroup
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties:
      DBSubnetGroupDescription: !Sub '${AppShortName}-${EnvName}-postgres-subnetgroup'
      DBSubnetGroupName: !Sub '${AppShortName}-${EnvName}-postgres-subnetgroup'
      SubnetIds: !Ref DBSubnetIds
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"

# PostgreSQL DB Parameter Group
  pgsqldbparametergroup:
    Type: AWS::RDS::DBParameterGroup
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]
    Properties: 
      Description: !Sub '${AppShortName}-${EnvName}-postgres-dbparametergroup'
      DBParameterGroupName: !Sub '${AppShortName}-${EnvName}-postgres-dbparametergroup'
      Family: !Ref PgsqlDBParameterGroupFamily
      Tags:
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"

  pgsqlinstance:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: !If [IsProduction, Retain, Delete]
    UpdateReplacePolicy: !If [IsProduction, Retain, Delete]  
    Properties:
      StorageEncrypted: true
      MonitoringRoleArn: !GetAtt dbenhancedmonitoringrole.Arn
      PreferredBackupWindow: "16:00-17:00"
      MonitoringInterval: 60
      DBParameterGroupName: !Ref pgsqldbparametergroup
      NetworkType: "IPV4"
      CopyTagsToSnapshot: true
      Engine: !Ref PgsqlDBEngine
      PerformanceInsightsKMSKeyId: !GetAtt pgsqlmonitoringencryptionkmskey.Arn
      LicenseModel: "postgresql-license"
      EngineVersion: !Ref PgsqlEngineVersion
      StorageType: !Ref PgsqlStorageType
      KmsKeyId: !GetAtt pgsqlencryptionkmskey.Arn
      DBInstanceClass: !Ref PgsqlDBInstanceClass
      PerformanceInsightsRetentionPeriod: !If [IsProduction, '31', '7']
      PreferredMaintenanceWindow: "sat:18:00-sat:19:00"
      EnablePerformanceInsights: true
      AutoMinorVersionUpgrade: true
      DBSubnetGroupName: !If
        - CreateNewSubnetGroup
        - !Ref pgsqldbsubnetgroup
        - !Ref ExistingSubnetGroupName
      DeletionProtection: true
      Iops: !If 
        - HasStorageIops
        - !Ref PgsqlStorageIops
        - !Ref "AWS::NoValue"
      DBInstanceIdentifier: !Sub '${AppShortName}-${EnvName}-rdspostg'
      AllocatedStorage: !Ref PgsqlAllocatedStorage
      CACertificateIdentifier: "rds-ca-rsa4096-g1"
      ManageMasterUserPassword: false
      VPCSecurityGroups:
      - !GetAtt pgsqldbsecuritygroup.GroupId
      MasterUsername: !Ref PgsqlMasterUsernameDev
      MasterUserPassword: !Ref PgsqlMasterUserPasswordDev
      EnableIAMDatabaseAuthentication: !Ref EnableIAMDatabaseAuthentication
      MaxAllocatedStorage: !Ref PgsqlMaxAllocatedStorage
      BackupRetentionPeriod: !If [IsProduction, '35', '7']
      PubliclyAccessible: false
      EnableCloudwatchLogsExports:
      - "postgresql"
      - "upgrade"
      Port: !If [IsProduction, !Ref PgsqlProdDBPort, !Ref PgsqlNProdDBPort]
      MultiAZ: !Ref MultiAZ
      AvailabilityZone: !If [IsMultiAZ, !Ref "AWS::NoValue", !Ref AvailabilityZone ]
      Tags:
        - Key: BACKUP-DAILY
          Value: 'YES'
        - Key: BACKUP-WEEKLY
          Value: 'YES'
        - Key: BACKUP-MONTHLY
          Value: 'YES'
        - Key: BACKUP-YEARLY
          Value: 'YES'
        - Key: BACKUP-CONTINUOUS
          Value: 'YES'
        - Key: "IaCVersion"
          Value: "InfraPlatform-rdspostg-v5.2"

# SSM Parameter store for PostgreSQL Instance:
  parameterpgsql:
    Type: AWS::SSM::Parameter
    DependsOn: pgsqlinstance
    Properties:
      Name: !Sub ${AppShortName}-${EnvName}-parameter-pgsqlinstancename
      Description: !Sub ${AppShortName}-${EnvName}-parameter-pgsqlinstancename
      Value: !Ref pgsqlinstance
      Type: String
      Tags:
        IaCVersion: "InfraPlatform-rdspostg-v5.2"